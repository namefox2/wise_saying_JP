pluginManagement {
    def rnPluginDir = file("../node_modules/@react-native/gradle-plugin")
    if (rnPluginDir.exists()) {
        // node_modules 있으면 실제 React Native 플러그인 사용
        includeBuild(rnPluginDir)
    } else {
        // node_modules 없으면 no-op stub 사용 (Android Studio Gradle Sync 성공용)
        includeBuild(file("rn-stub"))
    }
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins { id("com.facebook.react.settings") }

def rnPluginDir = file("../node_modules/@react-native/gradle-plugin")
if (rnPluginDir.exists()) {
    extensions.configure(com.facebook.react.ReactSettingsExtension) { ex ->
        ex.autolinkLibrariesFromCommand()
    }
    includeBuild('../node_modules/@react-native/gradle-plugin')
}

rootProject.name = 'KotobaNoTakarabako'
include ':app'
